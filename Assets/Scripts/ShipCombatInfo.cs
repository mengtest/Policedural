using UnityEngine;
using System.Collections;

public class ShipCombatInfo : MonoBehaviour {

    public float ShootSize = 1.0f;
    public float ShootLifeTime = 1.0f; // seconds ?
    public float ShootForce = 1.0f; 
    public float TimeBetweenShoots = 1.0f; // seconds ?

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
